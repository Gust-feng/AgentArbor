# 界面原型

本目录保存已经明确归属的 AgentArbor 工作台界面原型，用作产品界面与信息架构讨论材料。

## 目录说明

- `AgentArbor工作台重设计原型/`：普通 Agent 工作台重设计静态原型，包含空状态、对话进行中、设置面板和工具展示预览。

## 边界

- 这里不是运行时代码入口，当前应用不会自动消费这些 HTML / CSS / design 文件。
- 原型进入正式实现前，必须先转化为 `src/app/panel-ui/` 中的真实组件、状态契约和测试。
- 原型更新必须说明对应的界面假设，避免成为无主静态资产。
