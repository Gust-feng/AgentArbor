# 看板

最后更新：2026-05-03

本看板从 `.trellis/tasks/` 派生，并吸收正式开发指南中的方向性约束。若本页和 Trellis 状态不一致，以 Trellis 为准，并刷新本页。

## 当前指针

当前关注任务：

- `.trellis/tasks/05-03-underground-evidence-ledger-attribution/`

当前判断：地下组织已具备消息驱动运行单元、多候选候选池和交叉裁决；本轮把方向结论从“能收束”推进到“可追溯、可审计、可解释”。重点是 Evidence Ledger、CandidateComparison attribution、ConvergenceReport evidence ref、Direction Handoff `evidence-index.md` 和 Observation evidence summary，不接 LLM，不新增 UI / HTTP / 数据库 / 外部协议适配，也不写 repo-root `.agentarbor/` 运行资产。

## 当前任务

| 任务 | 状态 | 下一步 | Trellis |
| --- | --- | --- | --- |
| 地下证据账本与裁决归因硬化 | 进行中 | 硬化 `UndergroundEvidenceLedger`、rootlet output / comparison / decision / report evidence refs、Direction Handoff `evidence-index.md` 和 Observation evidence ledger 摘要；保持地下-only 7 步 EventLog 与完整 demo 18 步顺序不变。 | [PRD](../../.trellis/tasks/05-03-underground-evidence-ledger-attribution/prd.md) / [状态](../../.trellis/tasks/05-03-underground-evidence-ledger-attribution/task.json) |

阻塞点：无。当前实现仍使用 deterministic runtime，不接真实 provider，不写 repo-root `.agentarbor/`。

验收判断：`pnpm build` / `pnpm test` 必须通过；`pnpm demo:underground -- "构建任务管理平台，包含测试和监控，不接数据库"` 必须保持 7 步地下-only EventLog；`pnpm demo` 必须保持 18 步完整闭环；每个 rootlet output、candidate comparison、convergence decision、Direction Handoff evidence index 和 Observation evidence summary 都要能追溯 evidence refs。

## 前置任务

| 任务 | 结果 | 对当前的意义 | Trellis |
| --- | --- | --- | --- |
| Bootstrap Guidelines | 已完成 Trellis 初始化规范收束；后端/前端实现规范明确延后，不再保留英文空模板。 | 后续 AI 不应从空 `src/` 编造框架、目录、测试或实现约定。 | [PRD](../../.trellis/tasks/00-bootstrap-guidelines/prd.md) / [状态](../../.trellis/tasks/00-bootstrap-guidelines/task.json) |
| Add OpenCode Compatibility | 已完成 OpenCode 适配，并明确 `.trellis/` 是共享工作流核心，`.opencode/` 是平台适配层。 | 后续跨平台兼容不能把 OpenCode 或 Codex 文件反向写成 AgentArbor 产品源数据。 | [PRD](../../.trellis/tasks/04-30-opencode-compatibility/prd.md) / [状态](../../.trellis/tasks/04-30-opencode-compatibility/task.json) |
| Formal Development Guide Integration | 已完成正式开发指南主线和树形运行架构整合。 | 当前产品定位、文档树和后续实现边界都应以 `docs/开发指南/` 为主入口。 | [PRD](../../.trellis/tasks/04-30-formal-development-guide-integration/prd.md) / [状态](../../.trellis/tasks/04-30-formal-development-guide-integration/task.json) |
| Architecture Constraint Engineering Docs | 已完成 ADR-0017 约束工程新增、开发指南 Constraint / ConstraintRef 契约补齐。 | 当前架构迁移必须保留 hard constraint、soft constraint、preference、Path Bias 和治理门边界。 | [PRD](../../.trellis/tasks/05-01-architecture-constraint-engineering-docs/prd.md) / [状态](../../.trellis/tasks/05-01-architecture-constraint-engineering-docs/task.json) |
| V0.2-V0.5 地下 runtime 基线 | 已形成 package store、地下辐射生长、Observation Kernel、用户澄清升级、澄清恢复和 package lineage 的确定性内存基线；部分 Trellis 任务状态仍显示 in_progress，但不再并列为下一阶段主线。 | 下一步应把这些能力收束成地下单环，而不是提前打通地上回调地下。 | [V0.2.1](../../.trellis/tasks/05-01-v0-2-1-modular-hardening/task.json) / [V0.3](../../.trellis/tasks/05-01-v0-3-underground-observation/task.json) / [V0.4](../../.trellis/tasks/05-01-v0-4-observation-kernel-refinement/task.json) / [V0.5](../../.trellis/tasks/05-01-v0-5-underground-user-clarification-escalation/task.json) |
| Fix P1 quality audit regressions | 已完成四个 P1 修复和回归测试：non-blocking unknown 无 handoff candidates 且预算耗尽时 deterministic `stopped`；awaiting_user package 篡改为 approved 会被 validation 和 Aboveground planning 阻断；hard constraint 的 proposed / governance_review 等未满足策略不会进入 Assigned；EventLog `list()` / `replay()` 不暴露内部 message 引用。 | 澄清恢复谱系可以建立在更严格的 convergence、package validation、constraint guard 和 EventLog 不变量之上。 | [PRD](../../.trellis/tasks/05-02-fix-p1-quality-audit-regressions/prd.md) / [状态](../../.trellis/tasks/05-02-fix-p1-quality-audit-regressions/task.json) |
| 地下组织最小使用闭环 | 已完成并分批提交：`runUndergroundDirectionSession` 支持存储注入和显式输出目录；awaiting_user 可通过 deterministic 自动回答恢复为 approved v2；`demo:underground --auto-answer` / `--out` 可验收；Trellis 共享事实源已纳入 git 边界。 | 真实 AI 接入可以先服务地下目标理解与 rootlet 候选生成，但必须通过智能通道。 | [PRD](../../.trellis/tasks/05-02-underground-minimal-usable-loop/prd.md) / [状态](../../.trellis/tasks/05-02-underground-minimal-usable-loop/task.json) |
| 智能通道与模型接入边界文档 | 已完成并分批提交：新增 ADR-0020、模型契约、后端可执行规范，并更新系统架构、模块划分、AI/确定性边界和任务看板。 | 当前运行内核开发必须遵守智能通道是唯一模型入口、AI 输出不直接成为事实源、provider adapter 不污染领域模型。 | [PRD](../../.trellis/tasks/05-02-intelligence-channel-docs/prd.md) / [状态](../../.trellis/tasks/05-02-intelligence-channel-docs/task.json) |
| 智能通道最小运行内核 | 已完成并分批提交：新增 `domain/intelligence`、`kernel/intelligence`、deterministic fake provider、OpenAI-compatible Chat Completions adapter、模型事件和地下候选层接入；默认 demo 不触发真实网络。 | 地下 agent 集群可以把 `IntelligenceChannel` 作为 agent 能力来源，但不能让模型输出绕过 invocation、candidate pool、Convergence Judge 和 package validation。 | [PRD](../../.trellis/tasks/05-02-intelligence-channel-runtime/prd.md) / [状态](../../.trellis/tasks/05-02-intelligence-channel-runtime/task.json) |
| 地下 Agent 集群调度内核 | 已完成并分批提交：新增地下 agent cluster contract、app cluster runtime、rootlet output invocation 守卫、cluster plan / invocation observation 投影。 | 当前消息驱动调度建立在这个 agent cluster 形态之上；handler 发布的事件必须能回溯到具体 agent invocation。 | [PRD](../../.trellis/tasks/05-02-underground-agent-cluster-runtime/prd.md) / [状态](../../.trellis/tasks/05-02-underground-agent-cluster-runtime/task.json) |
| 地下消息驱动调度内核 | 已完成并分批提交：地下-only session 只发布 `goal.received`，后续由消息驱动 dispatcher 推进到 `direction_handoff.completed` / `user_approval.requested`；重复消息、跳阶段和 max step 失控有守卫。 | 当前运行单元化不再重做消息驱动能力，而是把 dispatcher 中的业务阶段拆成独立 agent runtime units，降低中央协调器职责。 | [PRD](../../.trellis/tasks/05-02-underground-message-driven-runtime/prd.md) / [状态](../../.trellis/tasks/05-02-underground-message-driven-runtime/task.json) |
| 地下 Agent 运行单元化 | 已完成并分批提交：地下运行拆成 `IntentCoreAgent`、`GrowthGovernorAgent`、`CandidatePoolAgent`、`ConvergenceJudgeAgent`、`HandoffStewardAgent` 和动态 `RootletAgent`；Runner 只负责生命周期、消息泵、动态根须创建和终态等待。 | 当前多候选探索建立在真实 agent runtime unit 之上，EventLog 的地下阶段必须能回溯到具体 agent。 | [PRD](../../.trellis/tasks/05-03-underground-agent-runtime-units/prd.md) / [状态](../../.trellis/tasks/05-03-underground-agent-runtime-units/task.json) |
| 地下根须多候选探索与交叉裁决对齐 | 已完成并分批提交：单个 rootlet invocation 可按预算产出多个候选；CandidatePool 按 rootlet kind 分组；Convergence Judge 输出推荐、合并、淘汰、用户确认和地上参考；Direction Handoff 的 options / decisionRecord / riskRegister 承接完整裁决。 | 当前证据账本硬化建立在多候选和交叉裁决之上；下一步不再扩展候选数量，而是把来源归因和证据索引补成可审计链路。 | [PRD](../../.trellis/tasks/05-03-underground-multi-candidate-convergence/prd.md) / [状态](../../.trellis/tasks/05-03-underground-multi-candidate-convergence/task.json) |

## 未来方向

这些条目是方向提示，不是已启动任务。只有用户确认并创建或启动 Trellis 任务后，才进入“当前任务”。

| 方向 | 为什么重要 | 启动条件 |
| --- | --- | --- |
| repo-root `.agentarbor` 运行资产生命周期 | 当前只证明内存 store 和显式临时目录文件系统 store，不创建真实 repo-root `.agentarbor/` 资产；后续仍需定义真实出生条件、提交策略、读写权限和治理边界。 | 方向交接包契约稳定后，用户确认允许创建真实运行资产，并明确输出根目录、出生依据和哪些内容进入提交基线。 |
| 地上单环稳定化 | 地上中枢需要能独立从 approved package 生成 Growth Plan、Workflow IR、执行组织和验证门，先证明单环，再讨论回调地下。 | 地下独立闭环产出边界稳定后，用户确认进入地上单环专题。 |
| Nutrient Request 与 Growth Plan Revision | 地上执行遇到养料不足或验证失败时，未来需要能向地下中枢请求养料并修订计划；这是跨环节协同，不是地下单环的立即下一步。 | 地下单环和地上单环均稳定后，用户确认打通跨环节链路。 |
| Constraint / ConstraintRef 深化 | 首个最小运行内核会证明 hard constraint 阻断或确认门；后续还需要把软约束偏离、preference 排序和治理沉淀做成更完整机制。 | 最小内存闭环通过后，用户确认进入约束执行与验证细化。 |
| Fruit 与 Capability Asset 治理 | 子 agent 是否能脱离母体，取决于成熟、验证、权限收敛和谱系记录。 | 开始定义能力资产注册、导出、退役和权限边界。 |

## 维护提示

- 完成任务后，只把关键结果压缩进“前置任务”，不为每个任务新建看板文件。
- 当前任务只写正在推进或需要马上决策的内容。
- 未来方向只写方向、价值和启动条件，不写成承诺排期。
- 详细 PRD、阶段记录和工作流状态回到 Trellis 查看。
