# 阶段验收边界

## 文档阶段验收

当前文档阶段完成标准：

- 产品定位清楚：AgentArbor 是桌面通用 Agent。
- 当前事实源是 ADR-0022，而不是旧原生概念树。
- 开发指南、历史看板、研究资料和架构设计边界清楚。
- `.agentarbor` 已降级为 Plan Package 的实现/存储形态，不再作为独立产品节点。
- Task Soil / Global Soil 双层土壤已经明确。
- Shared Agent Kernel + Two Product Runtimes 已经明确。
- 四层权限模型已经明确。
- Main Canvas 与 Observation Panel 的解释分工已经明确。
- MVP 禁止递归派生 child agent。
- Fruits 和 Global Soil 之间存在明确 Governance Pipeline。
- 索引全部指向真实文件。

## 实现阶段验收

后续默认 agent 打磨的最小验收标准：

- 能从 Desktop Shell 输入自然语言任务。
- 能持续对话，并把最近可见对话作为模型上下文传递。
- 能通过 `AgentTurnRuntime` 完成模型工具循环。
- 能在删除、命令执行、外部提交和高影响动作前请求确认；常规工作区文件创建、编辑、写入不默认打断用户。
- 能持久化会话、运行、事件和确认请求的安全投影。
- 能展示普通回答、工具引用、待确认事项、错误和结果摘要。
- 能执行至少一个样例任务。
- 能记录安全事件和必要谱系。
- 能运行验证并给出通过或失败证据。
- 能阻止 raw prompt、raw provider response、raw tool output、stdout/stderr、文件正文、secret、token 或 hidden reasoning 进入普通投影。
- 能在主工作区展示结果级解释。
- 能在详情或诊断中展示 agent 过程级安全投影。

## 不合格信号

出现以下情况，说明方向偏离：

- 只有聊天回答，没有工具循环、确认、安全事件、持久化或结果展示。
- 只生成 prompt 或角色文件。
- 只有文档，没有可执行规格。
- 只有运行，没有验证。
- 只有生成，没有谱系。
- 只有平台适配，没有 AgentArbor 原生模型。
- 默认入口重新暴露 deep / Agent 集群，或普通请求被工程规则自动升级到 Underground。
- `.agentarbor` 被恢复成独立产品节点或最终资产仓库。
- Fruit 绕过 Governance Pipeline 进入 Global Soil。
- 子 agent 能绕过权限和治理直接脱离母体。
- 工程边界替 agent 判断目标理解、候选排序、是否继续探索或方向综合。
