# 看板

最后更新：2026-05-01

本看板从 `.trellis/tasks/` 派生，并吸收正式开发指南中的方向性约束。若本页和 Trellis 状态不一致，以 Trellis 为准，并刷新本页。

## 当前指针

Trellis 当前指向：

- `.trellis/tasks/05-01-seed-root-trunk-boundary/`

当前判断：正在把 Seed / Root / Trunk 边界探索收束为 AgentArbor 原生概念树架构，并把活跃开发指南迁移到 `Soil -> Underground Center -> .agentarbor -> Aboveground Center -> Fruits -> Governance -> Soil`。

## 当前任务

| 任务 | 状态 | 下一步 | Trellis |
| --- | --- | --- | --- |
| AgentArbor 原生概念树架构文档调整 | in_progress | 进入提交前确认：文档迁移、规范沉淀和验证已完成，等待提交计划确认。 | [PRD](../../.trellis/tasks/05-01-seed-root-trunk-boundary/prd.md) / [状态](../../.trellis/tasks/05-01-seed-root-trunk-boundary/task.json) |

阻塞点：无任务级阻塞；`.agentarbor/` 存在被 `.gitignore` 忽略的本地旧资产，但没有进入 git 基线。

验收判断：ADR、根入口、开发指南、架构索引、模型契约、工程模块划分和 Trellis 治理指南已统一到原生概念树。

## 前置任务

| 任务 | 结果 | 对当前的意义 | Trellis |
| --- | --- | --- | --- |
| Bootstrap Guidelines | 已完成 Trellis 初始化规范收束；后端/前端实现规范明确延后，不再保留英文空模板。 | 后续 AI 不应从空 `src/` 编造框架、目录、测试或实现约定。 | [PRD](../../.trellis/tasks/00-bootstrap-guidelines/prd.md) / [状态](../../.trellis/tasks/00-bootstrap-guidelines/task.json) |
| Add OpenCode Compatibility | 已完成 OpenCode 适配，并明确 `.trellis/` 是共享工作流核心，`.opencode/` 是平台适配层。 | 后续跨平台兼容不能把 OpenCode 或 Codex 文件反向写成 AgentArbor 产品源数据。 | [PRD](../../.trellis/tasks/04-30-opencode-compatibility/prd.md) / [状态](../../.trellis/tasks/04-30-opencode-compatibility/task.json) |
| Formal Development Guide Integration | 已完成正式开发指南主线和树形运行架构整合。 | 当前产品定位、文档树和后续实现边界都应以 `docs/开发指南/` 为主入口。 | [PRD](../../.trellis/tasks/04-30-formal-development-guide-integration/prd.md) / [状态](../../.trellis/tasks/04-30-formal-development-guide-integration/task.json) |
| Architecture Constraint Engineering Docs | 已完成 ADR-0017 约束工程新增、开发指南 Constraint / ConstraintRef 契约补齐。 | 当前架构迁移必须保留 hard constraint、soft constraint、preference、Path Bias 和治理门边界。 | [PRD](../../.trellis/tasks/05-01-architecture-constraint-engineering-docs/prd.md) / [状态](../../.trellis/tasks/05-01-architecture-constraint-engineering-docs/task.json) |

## 未来方向

这些条目是方向提示，不是已启动任务。只有用户确认并创建或启动 Trellis 任务后，才进入“当前任务”。

| 方向 | 为什么重要 | 启动条件 |
| --- | --- | --- |
| `.agentarbor` 方向交接包契约实现 | 当前架构把 `.agentarbor` 定位为地下到地上的方向交接层，需要未来用真实 schema 和读写规则证明。 | 用户确认进入首个运行时契约实现。 |
| 地下与地上生长机制深化 | 当前架构已确立静态概念树，后续需要单独分析地下如何长出方向、地上如何长出执行组织和果实，避免把动态机制混进本轮架构迁移。 | 当前原生概念树架构提交后，用户确认进入动态生长机制专题。 |
| Nutrient Request 与 Growth Plan Revision | 地上执行遇到养料不足或验证失败时，需要能向地下中枢请求养料并修订计划。 | 设计最小 runtime 状态机或 Workflow IR 时。 |
| Constraint / ConstraintRef 最小实现 | 约束工程已经成为文档契约，后续代码需要证明约束可阻断、可偏离记录、可验证。 | 进入最小运行契约或首个 runtime demo 实现时。 |
| Fruit 与 Capability Asset 治理 | 子 agent 是否能脱离母体，取决于成熟、验证、权限收敛和谱系记录。 | 开始定义能力资产注册、导出、退役和权限边界。 |
| 首个实现任务的 Trellis 启动纪律 | 当前仓库仍无运行时代码，下一次进入实现不能绕过 Trellis 任务源。 | 用户确认进入具体实现或协议落地时，先创建或启动对应 Trellis 任务。 |

## 维护提示

- 完成任务后，只把关键结果压缩进“前置任务”，不为每个任务新建看板文件。
- 当前任务只写正在推进或需要马上决策的内容。
- 未来方向只写方向、价值和启动条件，不写成承诺排期。
- 详细 PRD、阶段记录和工作流状态回到 Trellis 查看。
